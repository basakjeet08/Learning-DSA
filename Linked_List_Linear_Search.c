#include <stdio.h>
#include <stdlib.h>
struct Node{
    int data ;
    struct Node* next;
};
void getData(struct Node*);
int main(){
    struct Node *head, *new;
    new = (struct Node*)malloc(sizeof(struct Node));
    head = ptr = new;



    return 0 ;
}